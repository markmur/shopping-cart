import { Theme } from "styled-system";

const theme: Theme = {
  colors: {
    background: "#f8f8ff",
    border: "#dedef3",
    nav: "#000",
    navColor: "#fff",
    success: "green"
  }
};

export default theme;
